<?php
/**
 * Copie ce fichier en `config.php` (gitignoré) et renseigne tes valeurs.
 * Aucun secret n'est versionné dans ce dépôt.
 */
const DB_HOST = 'localhost';
const DB_NAME = 'your_database';
const DB_USER = 'your_db_user';
const DB_PASS = 'your_db_password';

// Identifiants Basic Auth protégeant la page (les données clients passent par cette auth)
const ACCESS_USER = 'admin';
const ACCESS_PASS = 'change-me';
